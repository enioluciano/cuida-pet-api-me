name: cuidapet_api_me
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'


dependencies:

  path: ^1.8.0
  shelf: ^1.2.0
  dotenv: ^3.0.0
  mysql1: ^0.19.2
  get_it: ^7.2.0
  logger: ^1.1.0

  shelf_router: ^1.1.2

  injectable: ^1.2.2
 
  args: ^2.0.0

  

  jaguar_jwt: ^3.0.0

dev_dependencies:
  build_runner: ^2.1.7
  injectable_generator: ^1.2.2
  shelf_router_generator: ^1.0.2

  http: ^0.13.0
  lints: ^1.0.0
  test_process: ^2.0.0
  test: ^1.15.0
